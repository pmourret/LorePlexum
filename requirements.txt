colorama==0.4.6
emoji==2.14.0
python-dotenv==1.0.1
pyperclip==1.11.0

# --- Génération PDF ---
# WeasyPrint remplace pdfkit/wkhtmltopdf : pur Python, plus AUCUN binaire externe
# à installer. Il s'appuie sur des bibliothèques système (Pango/Cairo/gdk-pixbuf),
# fournies par l'image Docker en production. En dev sous Windows, ces libs GTK ne
# sont pas présentes par défaut : l'app démarre quand même, seule la génération du
# PDF est indisponible (échec non bloquant). Voir la doc WeasyPrint pour Windows.
weasyprint==63.0

# --- Interface web (FastAPI + HTMX) ---
fastapi==0.115.6
uvicorn==0.34.0
jinja2==3.1.4
python-multipart==0.0.20
# Réglages de déploiement typés (backend/app/config.py).
pydantic-settings==2.7.0
